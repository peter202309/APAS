# 航空价格自动化采集与 AI 智能分析系统 (APAS) 开发文档 (V1.0)

## 1. 项目概述
本系统旨在实现对 ITA Matrix 平台机票价格的自动化采集，支持多机并发、数据连续查询、可视化展示，并利用 AI 技术对东航 (MU) 及竞争对手的价格进行深度分析与调价建议。

## 2. 技术栈选取
- **自动化引擎**: Python 3.10+ & Playwright (集成 Stealth 插件绕过检测)。
- **数据处理**: Pandas (数据清洗、聚合)。
- **后端框架**: FastAPI (用于任务调度与多机协同)。
- **前端界面**: React + Tailwind CSS + ECharts (高级感、动态图表仪表盘)。
- **AI 引擎**: Gemini 1.5 Pro / GPT-4o API (用于趋势预测与策略生成)。
- **任务队列**: Redis + Celery (支持分布式并发)。

---

## 3. 系统架构设计

### 3.1 核心逻辑架构
1.  **用户输入层**: 提供 GUI/Web 界面，输入查询条件（始发地、目的地、日期范围、航司偏好）。
2.  **调度控制层**: 
    *   将长日期需求拆分为多个任务。
    *   分发任务给本地或远程 Worker 节点。
3.  **采集执行层 (Playwright)**:
    *   模拟用户在 ITA Matrix 填表。
    *   执行“日历模式”搜索。
    *   **提取逻辑**: 仅针对搜索结果显示的**第一个完整月份**进行网格取值。
4.  **数据持久化**: 实时生成 CSV 并在后端数据库（SQLite/PostgreSQL）留存历史快照。
5.  **AI 分析层**: 调用 LLM 对比多家航司价格差值，计算 MU 指数。

---

## 4. 功能模块详细说明

### 4.1 自动化引擎模块 (Module - Scraper)
*   **支持参数集**: 
    *   **行程类型**: 单程 (One-Way) / 往返 (Round Trip)。
    *   **航点控制**: 始发地、目的地、Routing Codes (航司黑白名单)、Extension Codes。
    *   **日期控制**: 价格日历起始日期、停留天数 (Nights)。
    *   **偏好设置**: 停站次数 (Stops)、舱位等级 (Cabin)、销售地 (Sales City)、币种控制。
*   **视觉算法**: 
    *   识别日历网格坐标。
    *   匹配价格文本（如 `CA$540`）与对应日期。
    *   自动识别价格颜色（区分最低价与普通价）。
*   **自适应策略**: 采用语义识别，当网页布局微调时，通过 AI 重新定位“Search”按钮和日期列表。

### 4.2 数据管理与日志 (Module - Data & Log)
*   **CSV 结构**:
    *   `Search_Date`: 查询执行日期。
    *   `Flight_Date`: 航班目标日期。
    *   `Airline`: 航司名称。
    *   `Price`: 提取的价格数值。
    *   `Currency`: 货币。
    *   `Status`: 采集状态（成功/漏查/无航线）。
*   **查询日志**: 记录每台计算机的运行状态、抓取条数、耗时、报错截屏。

### 4.3 界面与交互 (Module - UI)
*   **设置页面**: 代理 IP 设置、抓取频率、备份路径配置。
*   **监控台**: 实时显示多台电脑的并发进度条。
*   **数据中心**: 一键备份配置和已存储的所有 CSV。

### 4.4 进阶：多机协同与安全 (Module - Cluster)
*   **分布式锁**: 确保不同机器不会抓取同一个日期，避免资源浪费。
*   **审计日志**: 详细记录“谁、在何时、修改了哪个采集规则”。

### 4.5 进阶：AI 决策大脑 (Module - AI Intel)
*   **数据模型**: 
    *   输入：MU 价格, 市场均价, 竞品最低价, 距离起飞天数。
    *   输出：调价建议（保持/适度上调/建议促销）、原因分析。
*   **可视化看板**: 
    *   **波折图**: 价格随日期的波动趋势。
    *   **热力图**: 不同舱位的价格密度分布。

---

## 5. 开发阶段划分

*   **第一阶段 (PoC)**: 实现单机 Playwright 脚本，成功在 ITA Matrix 提取一个月的数据并导出 CSV。
*   **第二阶段 (MVP)**: 完成基础 Web 界面，支持参数输入、连续日期查询和日志记录。
*   **第三阶段 (Enterprise)**: 实现多机并发管理后台、操作审计日志。
*   **第四阶段 (AI Plus)**: 集成 AI 分析模块，生成 MU 专属建议报告，完成数据备份功能。

---

## 6. 安全与风险预案
1.  **反爬对策**: 
    *   随机 User-Agent 轮换。
    *   操作间隙模拟真人随机停顿。
    *   支持高匿名住宅代理池。
2.  **数据安全**: 定时自动备份数据库到本地和云端，防止因系统错误导致数据丢失。
